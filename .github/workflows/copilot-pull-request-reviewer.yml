name: copilot-pull-request-reviewer
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dummy to prevent billing lock errors for copilot-pull-request-reviewer check"
