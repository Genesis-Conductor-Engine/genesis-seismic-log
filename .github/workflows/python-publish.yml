name: Upload Python Package

on: [workflow_dispatch]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Dummy Step
      run: echo "This workflow is disabled to prevent billing issues."
