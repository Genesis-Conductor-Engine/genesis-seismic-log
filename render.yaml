services:
  - type: web
    name: seismic-log
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn seismic_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
