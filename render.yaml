services:
  - type: web
    name: seismic-service
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python simple_seismic_server.py
    envVars:
      - key: PORT
        value: 10000
