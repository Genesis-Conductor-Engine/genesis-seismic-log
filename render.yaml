services:
  - type: web
    name: genesis-seismic-log
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 simple_seismic_server.py
    envVars:
      - key: PORT
        value: 8003
